[build]
  publish = "."

# Edge function for /api/chat
[[edge_functions]]
  path = "/api/chat"
  function = "chat"

# SPA fallback - serve index.html for all routes
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
